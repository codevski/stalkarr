meta {
  name: Trigger Stalk
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/sonarr/stalk
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Triggers a stalk job. Currently a stub returns {"status": "stalking"}.
}
