meta {
  name: Trigger Hunt
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/sonarr/hunt
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Triggers a hunt job. Currently a stub returns {"status": "hunting"}.
}
