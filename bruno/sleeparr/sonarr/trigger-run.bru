meta {
  name: Trigger Agent
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/sonarr/run
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Triggers a agent run. Currently a stub returns {"status": "running"}.
}
